var expect = chai.expect;

mocha.setup('bdd');

require.config({
    baseUrl: "../src"
});

describe('Virtual Scrolling Test', function() {
    it ("should do something", function(done) {
        done();
    });

});

